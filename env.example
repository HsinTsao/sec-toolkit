# Security Toolkit 环境变量配置
# 复制此文件为 .env 并修改配置

# ==================== 后端配置 ====================

# 数据库 URL
DATABASE_URL=sqlite+aiosqlite:///./data/toolkit.db

# JWT 密钥 (生产环境必须修改!)
JWT_SECRET_KEY=your-secret-key-change-this-in-production

# 调试模式
DEBUG=false

# CORS 配置 (允许的源，JSON 格式)
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","https://localhost:5173","https://localhost:3000","https://localhost","http://localhost"]

# ==================== SSL/HTTPS 配置 ====================

# 是否启用 SSL (true/false)
SSL_ENABLED=false

# SSL 证书路径 (相对于 backend 目录)
SSL_KEYFILE=../certs/server.key
SSL_CERTFILE=../certs/server.crt

# ==================== 前端配置 ====================

# 是否启用 HTTPS (true/false)
VITE_HTTPS=false

# 后端 API 地址 (HTTPS 模式时需要改为 https://localhost:8000)
VITE_API_URL=http://localhost:8000

# ==================== 使用说明 ====================
#
# HTTP 模式 (默认):
#   保持 SSL_ENABLED=false 和 VITE_HTTPS=false
#   运行: ./start.sh dev 或 ./start.sh run
#
# HTTPS 模式:
#   设置 SSL_ENABLED=true 和 VITE_HTTPS=true
#   设置 VITE_API_URL=https://localhost:8000
#   运行: ./start.sh ssl (先生成证书)
#   运行: ./start.sh dev-ssl 或 ./start.sh run-ssl
#
# Docker HTTPS 模式:
#   运行: ./start.sh prod-ssl
#
